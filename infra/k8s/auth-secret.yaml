apiVersion: v1
kind: Secret
metadata:
  name: auth-secret
type: Opaque
stringData:
  MONGO_URI: mongodb://auth-mongo-srv:27017/auth
  # ⚠️  Replace with a cryptographically secure secret in production.
  JWT_SECRET: dev-jwt-secret-change-me-in-production
